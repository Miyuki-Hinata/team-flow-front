# CLAUDE.md — TeamFlow フロントエンド実装ルール

このファイルは Claude Code がリポジトリで作業する際の**恒久ルール**です。
毎回の実装は、このルールに従って進めてください。

---

## 0. 最初に必ず読むもの

実装を始める前に、以下を必ず読んでから着手すること。

1. `docs/design/README.md` — デザインハンドオフ資料。**色・余白・角丸・フォント・各画面の仕様の唯一の正解**。ここに定義された値だけを使う。
2. `docs/design/TeamFlow_dc.html` — 参照用プロトタイプ。**見た目と挙動の正解**。インラインスタイルと状態遷移ロジックを読み取り、React コンポーネントとして書き直す（このHTMLの `<x-dc>` ランタイム自体は移植しない）。
3. `docs/frontend-overview-and-ui-audit.md` — 既存コードの実態調査。**どのページに何の重複があるか（ファイルパス・行番号つき）と、コンポーネント化の推奨着手順**を記載。「新規に何を作るか」ではなく「既存の素の `<button>`/`<input>`/`<select>`/`<span>` をどの共通コンポーネントに置き換えるか」の地図として使う。パート2の優先順位・着手順に従って進める。
4. 本ファイル（CLAUDE.md）— コードの作法。

役割分担：README が「**何を**作るか（デザインの理想）」、監査ファイルが「**今どうなっていて、どの順で直すか**（現状の地図）」、CLAUDE.md が「**どう書くか**（コードの一貫性）」を担当する。3つすべてを踏まえて実装すること。

---

## 1. お手本コンポーネント（既存の「正解の型」）

以下は開発者本人が実装し、理解済みの「正解の型」。**新規コンポーネントは必ずこれらと同じ書き方に揃える**こと。勝手に別の書き方を導入しない。

| お手本 | 何を参照するか |
|---|---|
| `components/ui/AISummaryCard.tsx` | ・propsを `type ○○Props = {...}` で型定義し、`({ a, b }: ○○Props)` で分割代入して受け取る<br>・汎用 `Card` を土台に組む<br>・`useState` での開閉状態の持ち方（必要な場合のみ） |
| `components/ui/Modal.tsx` | ・`isOpen` / `onClose` / `children` を受け取る汎用化パターン<br>・表示判断は `if (!isOpen) return null` でコンポーネント自身が持つ<br>・オーバーレイ自身に `onClick`、本体に `e.stopPropagation()` |
| `layouts/AppLayout.tsx` | ・`Outlet` によるページ差し込み<br>・レイアウトとページ本体の責任分離 |
| `components/ui/PatientCard.tsx` | ・`useTheme()` で theme を取得し、色は theme トークンから引く<br>・表示のみに責任を絞る（ナビゲーション等は呼び出し側の責任）<br>・単一責任 |

**参照の観点は「propsの受け取り方」「theme の使い方」「状態の持たせ方」「責任の分割」の4点**。ただ見た目を真似るのではなく、この4点の書き方を揃えること。
（例：`TaskCard` は表示のみなので `PatientCard` と同じく state を持たない。`Modal` の開閉パターンが不要なら入れない。）

---

## 2. 技術ルール

- **theme を使う**: 色・余白・角丸・フォントサイズは `theme.ts` のトークンを使う。README のデザイントークンに対応する。**ハードコード禁止**（`#0E9384` などを直接書かない。theme 経由で参照する）。
- **styled-components** で実装する（既存コンポーネントに揃える）。
- **props は分割代入**で受け取り、`type ○○Props` で型を定義する。
- **1コンポーネント1責任**。再利用できる粒度で分割する。今後の拡張性を考え、汎用化できるものは汎用化する（Badge / Card / Modal のように）。
- **日本語コメント**を付ける。特に「なぜこう書いたか」の意図を残す。
- **TypeScript の型を省略しない**。`any` を避ける。
- 未使用の import・変数は残さない。

---

## 3. 実装後のドキュメント化（必須）

各コンポーネントを実装したら、`docs/implementation/<コンポーネント名>.md` を作成し、以下を記録すること。

```
# <コンポーネント名>

## 何を作ったか
（1〜2行）

## なぜこの設計にしたか
（props構成・状態の持ち方・分割の理由。トレードオフがあれば明記）

## どのお手本に倣ったか
（AISummaryCard / Modal / AppLayout / PatientCard のどれの、どの観点を踏襲したか）

## 使用した theme トークン
（どの色・余白・角丸を使ったか）

## 面接で説明できるポイント
（このコンポーネントについて技術的に語れること）
```

このドキュメントは、開発者本人が「委譲したコードをなぜこう書いたか説明できる」状態になるためのもの。実装と同じ粒度で必ず残すこと。

---

## 4. やってはいけないこと（README のデザイン原則を踏襲）

- 定義外の色・余白・角丸・フォントサイズ/ウェイトを追加しない（READMEのトークンのみ）。
- 1つの色に2つの意味を持たせない（緑=完了/安定、赤=緊急）。
- 全カードを均質に並べない。緊急など重要情報にメリハリ（赤ストリップ+赤枠、最上段配置）をつける。
- 文字を詰め込まない。小さいボタンを大量に並べない。
- お手本と異なる独自の書き方を勝手に導入しない（一貫性を最優先）。

---

## 5. 作業の進め方

理解を最優先する。**一気にまとめて実装せず、以下のフェーズと粒度を厳守すること。**

### フェーズ0：全体把握と進捗ファイルの準備（最初に1回だけ）

1. Claude Design のモック（`docs/design/README.md`・`docs/design/TeamFlow_dc.html`、およびMCP接続時は Claude Design 側のデザイン）全体を確認する。
2. `docs/progress.md`（進捗管理ファイル）を新規作成する。
3. `docs/progress.md` に「実装が必要なページ一覧」を書き出す（チェックボックス形式）。README の Screens/Views と実ルーティングを突き合わせること。
4. `docs/progress.md` に「実装が必要なコンポーネント一覧」を書き出す（チェックボックス形式）。**既に実装済みのもの（Button, Badge, PriorityBadge, StatusBadge, Card, PatientCard, PatientIcon, Modal, AISummaryCard, AppLayout 等）は「実装済み」として明記し、再作成しない**。`docs/frontend-overview-and-ui-audit.md` の洗い出しと優先順位を土台にする。

### フェーズ1：コンポーネントを1つずつ実装

**コンポーネントは必ず1つずつ作る。** 監査ファイルの推奨着手順（依存の少ない土台から）に従う。

各コンポーネントについて、以下を1サイクルとする：

1. コンポーネントを1つ実装する（§1のお手本・§2の技術ルールに従う）。
2. `docs/implementation/<コンポーネント名>.md` を作成する（§3のテンプレート）。
3. `docs/progress.md` の該当コンポーネントにチェックを入れる。
4. **ここで一旦停止し、実装が完了したことを報告する。** 次のコンポーネントには自動で進まない。

※ 実装したコードの「理解確認・レビュー」は、開発者本人が別途（Claude Code外での対話で）行う。Claude Code 側は「実装」と「ドキュメント化」に徹し、解説で答えを提示しきる必要はない（開発者が自分で考える余地を残す）。開発者から「次へ」の指示が出たら、次のコンポーネントに進む。

### フェーズ2：ページを1つずつ実装

コンポーネントが揃った後、**ページも1つずつ実装する。**

1. ページを1つ実装する（揃ったコンポーネントを組み合わせる）。
2. `docs/progress.md` の該当ページにチェックを入れる。
3. 一旦停止して報告し、開発者の「次へ」を待つ。

### 共通ルール

- 実装が README / お手本と矛盾する場合や、判断に迷う場合は、勝手に進めず**その旨を明記して確認を求める**。
- バックエンドAPI（`http://localhost:8080` 等）のデータ構造は、READMEの「データモデル」および既存の `types/` を参照する。
- 既存コードの不整合（Dashboardのリンク先不一致、catch-all無し、権限チェックの非一貫性、Role未活用 等、監査ファイルの「仕様が曖昧だった箇所」参照）は **#18（UI整備）の範囲外**。今回は触らず、別Issueに委ねる。